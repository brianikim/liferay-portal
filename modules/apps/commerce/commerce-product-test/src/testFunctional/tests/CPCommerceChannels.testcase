@component-name = "portal-commerce"
definition {

	property browser.chrome.version = "139.0";
	property portal.release = "true";
	property portal.upstream = "true";
	property testray.main.component.name = "Product Info Management";

	setUp {
		CommerceConfiguration.commerceSetUp(minium = "true");

		task ("Enable private pages") {
			PagesAdmin.enablePrivatePages();
		}
	}

	tearDown {
		CommerceConfiguration.commerceTearDown();
	}

	@description = "This is a test for COMMERCE-10053. An error message shows up when the eligibility element is already linked"
	@priority = 3
	test CannotSelectShippingMethodEligibilitiesTwice {
		property portal.acceptance = "false";

		task ("Given an active delivery term is created") {
			CommerceJSONTermsAndConditionsAPI._addCommerceTermsAndConditions(termsType = "Delivery");
		}

		task ("And an order type is created") {
			CommerceJSONOrdersAPI._addOrderType(
				active = "true",
				orderTypeName = "Order Type Test");
		}

		task ("And the admin navigates to Minium Portal channel entry") {
			CommerceChannels.openChannelsAdmin();

			CommerceNavigator.gotoEntry(entryName = "Minium Portal");
		}

		task ("And Flat Rate's eligibility is set specifically to Delivery Term 1 and Order Type Test") {
			CommerceEntry.setEntryEligibility(
				deliveryTermsEligibility = "Delivery Term 1",
				orderTypesEligibility = "Order Type Test",
				shippingMethod = "Flat Rate",
				shippingOption = "Standard Delivery");
		}

		task ("When the admin selects the same order type eligibility once again") {
			CommerceEntry.setEntryEligibility(
				orderTypesEligibility = "Order Type Test",
				shippingOption = "Standard Delivery",
				unpublish = "true");
		}

		task ("Then an error message is shown") {
			Alert.viewErrorMessage(errorMessage = "The shipping fixed option qualifier already exists.");
		}

		task ("When the admin selects the same term eligibility once again") {
			CommerceEntry.setEntryEligibility(
				deliveryTermsEligibility = "Delivery Term 1",
				unpublish = "true");
		}

		task ("Then an error message is shown") {
			Alert.viewErrorMessage(errorMessage = "The shipping fixed option qualifier already exists");
		}
	}

	@description = "This is a test for COMMERCE-4270. CheckAvalaraTaxPresence"
	@priority = 3
	test CheckAvalaraTaxPresence {
		property portal.acceptance = "false";
		property portal.release = "false";

		CommerceNavigator.gotoPortlet(
			category = "Settings",
			portlet = "Avalara Connector");

		CommerceChannels.avalaraConnection();

		CommerceChannels.openChannelsAdmin();

		CommerceNavigator.gotoEntry(entryName = "Minium Portal");

		Click(
			key_entryName = "Avalara",
			locator1 = "CommerceEntry#CHANNELS_GENERAL_TAB_EDITS");

		SelectFrame.selectFrameNoLoading(locator1 = "CommerceEntry#IFRAME_SIDE_PANEL");

		AssertTextEquals(
			key_text = "Name",
			locator1 = "TextInput#ANY",
			value1 = "Avalara");

		AssertTextEquals(
			key_text = "Description",
			locator1 = "TextInput#ANY",
			value1 = "Avalara Type Rate Description");

		Check.checkToggleSwitch(
			key_toggleSwitchLabel = "Percentage",
			locator1 = "ToggleSwitch#ANY_TOGGLE_SWITCH");

		Check.checkToggleSwitch(
			key_toggleSwitchLabel = "Active",
			locator1 = "ToggleSwitch#ANY_TOGGLE_SWITCH");

		Button.clickSave();

		Alert.viewSuccessMessage();

		CommerceEntry.gotoMenuTab(menuTab = "Settings");

		AssertElementPresent(
			key_warningMessage = "Configure Credentials Before Continuing",
			locator1 = "Message#WARNING_SPECIFIC");

		CommerceEntry.gotoMenuTab(menuTab = "Avalara Tax Rates");

		AssertElementPresent(locator1 = "CommerceNavigation#EMPTY_SEARCH_CONTAINER");
	}

	@description = "[Please Update]"
	@ignore = "Test Stub"
	@priority = 4
	test CompleteCheckoutWithDefaultPaymentMethod {

		// TODO COMMERCE-8535 CompleteCheckoutWithDefaultPaymentMethod pending implementation

	}

	@description = "This is a test for COMMERCE-6105. EditMoneyOrderPaymentMethod"
	@priority = 3
	test EditMoneyOrderPaymentMethod {
		property portal.acceptance = "false";

		CommerceChannels.openChannelsAdmin();

		CommerceNavigator.gotoEntry(entryName = "Minium Portal");

		CommerceEntry.clickTableEntryEditButton(
			entryName = "Money Order",
			table = "Payment Methods");

		SelectFrame.selectFrameNoLoading(locator1 = "CommerceEntry#IFRAME_SIDE_PANEL");

		Type(
			locator1 = "TextInput#NAME",
			value1 = "Money Order");

		Type(
			locator1 = "TextInput#DESCRIPTION",
			value1 = "Test");

		Check.checkToggleSwitch(locator1 = "CommerceEntry#ACTIVE_CHECKBOX");

		Button.clickSave();

		Alert.viewSuccessMessage();
	}

	@description = "This is a test for COMMERCE-8386. SearchProductDisplayPage"
	@priority = 5
	test SearchProductDisplayPage {
		property portal.acceptance = "true";

		CommerceProducts.openProductsAdmin();

		CommerceNavigator.searchEntry(entryName = "Turbocharger");

		CommerceNavigator.gotoEntry(entryName = "Turbocharger");

		CommerceEntry.gotoMenuTab(menuTab = "Media");

		for (var number : list "106,107,108") {
			CommerceEntry.checkImageName(uploadFileName = "Minium_ProductImage_${number}.png");
		}

		CommerceEntry.gotoMenuTab(menuTab = "Visibility");

		CommerceEntry.assertCheckedSpecificToggle(name = "Channels");

		CommerceChannels.openChannelsAdmin();

		CommerceNavigator.gotoEntry(entryName = "Minium Portal");

		CommerceEntry.gotoMenuTab(menuTab = "Product Display Pages");

		Click(locator1 = "Button#PLUS");

		SelectFrame.selectFrameNoLoading(locator1 = "CommerceEntry#IFRAME_SIDE_PANEL");

		Button.click(button = "Select Product");

		SelectFrameTop();

		SelectFrame.selectFrameNoLoading(locator1 = "IFrame#MODAL_IFRAME");

		CommerceNavigator.searchEntry(entryName = "Turbocharger");

		AssertElementPresent(
			key_entry = "Turbocharger",
			locator1 = "CommerceNavigation#TD_ANY_ENTRY_NAME");

		CommerceNavigator.clearSearchBar();

		AssertElementNotPresent(
			key_entry = "Turbocharger",
			locator1 = "CommerceNavigation#TD_ANY_ENTRY_NAME");

		Click(
			key_pageNumber = 3,
			locator1 = "CommerceNavigation#PAGINATION_ANY_PAGE");

		AssertElementPresent(
			key_entry = "Turbocharger",
			locator1 = "CommerceNavigation#TD_ANY_ENTRY_NAME");
	}

	@description = "Verify all shipping options are available when orders are made using different order types given shipping option eligibility is set to All Order Types."
	@priority = 5
	test ShippingOptionEligibleForAnyOrderType {
		property custom.properties = "company.security.strangers.verify=false${line.separator}jsonws.web.service.paths.excludes=";
		property portal.acceptance = "true";

		task ("Given an account with a buyer user") {
			CommerceEntry.addAccountEntryUser(
				accountName = "Commerce Account",
				accountType = "Business",
				agreeToTermsAndAnswerReminderQuery = "true",
				createNewAccount = "true",
				requireReset = "false",
				userEmailAddress = "buyer@liferay.com",
				userFirstName = "Buyer",
				userLastName = "User",
				userRole = "Buyer",
				userScreenName = "buyeruser",
				userSiteMembership = "Minium");
		}

		task ("And given two active order types") {
			CommerceJSONOrdersAPI._addOrderType(
				active = "true",
				orderTypeDescription = "Test Order Type Description 1",
				orderTypeName = "Test Order Type 1");

			CommerceJSONOrdersAPI._addOrderType(
				active = "true",
				orderTypeDescription = "Test Order Type Description 2",
				orderTypeName = "Test Order Type 2");
		}

		task ("And given the buyer creates an order for each order type") {
			User.logoutPG();

			CommerceLogin.miniumLogin(
				password = PropsUtil.get("default.admin.password"),
				urlAppend = "web/minium",
				userEmailAddress = "buyer@liferay.com");

			CommerceAcceleratorsInitializer.createNewOrderFromPendingOrders(orderType = "Test Order Type 1");

			WaitForElementPresent(locator1 = "CommerceEntry#ACTIVE_ORDER_ID");

			var order1Id = selenium.getText("CommerceEntry#ACTIVE_ORDER_ID");

			CommerceAcceleratorsInitializer.createNewOrderFromPendingOrders(orderType = "Test Order Type 2");

			WaitForElementPresent(locator1 = "CommerceEntry#ACTIVE_ORDER_ID");

			var order2Id = selenium.getText("CommerceEntry#ACTIVE_ORDER_ID");
		}

		task ("When the user goes to checkout for the Test Order Type 2 order") {
			CommerceNavigator.gotoMiniumSidebarItem(menuItem = "Catalog");

			FrontStore.addInitializerProductToCart(
				productName = "U-Joint",
				productQuantity = 1);

			CommerceOrders.selectOrderFromAccountSelector(orderId = ${order2Id});

			Button.click(button = "Checkout");
		}

		task ("Then at the shipping method step, all active Shipping options should be available") {
			CommerceCheckout.initializersCheckout(
				newAddressAddress = "Test Address",
				newAddressCity = "Test City",
				newAddressCountry = "United States",
				newAddressName = "Address Name",
				newAddressZipCode = "Test Zip Code");

			AssertElementPresent(
				key_radioOption = "Standard",
				locator1 = "CommerceEntry#RADIO_BUTTON");

			AssertElementPresent(
				key_radioOption = "Expedited",
				locator1 = "CommerceEntry#RADIO_BUTTON");
		}

		task ("When the user goes to checkout for the Test Order Type 1 order") {
			CommerceNavigator.gotoMiniumSidebarItem(menuItem = "Catalog");

			CommerceOrders.selectOrderFromAccountSelector(orderId = ${order1Id});

			CommerceNavigator.gotoMiniumSidebarItem(menuItem = "Catalog");

			FrontStore.addInitializerProductToCart(
				productName = "U-Joint",
				productQuantity = 1);

			CommerceOrders.selectOrderFromAccountSelector(orderId = ${order1Id});

			Button.click(button = "Checkout");
		}

		task ("Then at the shipping method step, all active Shipping options should be available") {
			CommerceCheckout.initializersCheckout(
				newAddressAddress = "Test Address",
				newAddressCity = "Test City",
				newAddressCountry = "United States",
				newAddressName = "Address Name",
				newAddressZipCode = "Test Zip Code");

			AssertElementPresent(
				key_radioOption = "Standard",
				locator1 = "CommerceEntry#RADIO_BUTTON");

			AssertElementPresent(
				key_radioOption = "Expedited",
				locator1 = "CommerceEntry#RADIO_BUTTON");
		}
	}

	@description = "Verify users can configure shipping option eligibility to specific order types."
	@priority = 5
	test ShippingOptionEligibleForSpecificOrderType {
		property custom.properties = "company.security.strangers.verify=false${line.separator}jsonws.web.service.paths.excludes=";
		property portal.acceptance = "true";

		task ("Given an account with a buyer user") {
			CommerceEntry.addAccountEntryUser(
				accountName = "Commerce Account",
				accountType = "Business",
				agreeToTermsAndAnswerReminderQuery = "true",
				createNewAccount = "true",
				requireReset = "false",
				userEmailAddress = "buyer@liferay.com",
				userFirstName = "Buyer",
				userLastName = "User",
				userRole = "Buyer",
				userScreenName = "buyeruser",
				userSiteMembership = "Minium");
		}

		task ("And given two active order types") {
			CommerceJSONOrdersAPI._addOrderType(
				active = "true",
				orderTypeDescription = "Test Order Type Description 1",
				orderTypeName = "Test Order Type 1");

			CommerceJSONOrdersAPI._addOrderType(
				active = "true",
				orderTypeDescription = "Test Order Type Description 2",
				orderTypeName = "Test Order Type 2");
		}

		task ("And given an active shipping option's eligibility is set specifically to Test Order Type 1") {
			CommerceChannels.openChannelsAdmin();

			CommerceNavigator.gotoEntry(entryName = "Minium Portal");

			CommerceEntry.setEntryEligibility(
				orderTypesEligibility = "Test Order Type 1",
				shippingMethod = "Flat Rate",
				shippingOption = "Standard Delivery");
		}

		task ("And given the buyer creates an order for each order type") {
			User.logoutPG();

			CommerceLogin.miniumLogin(
				password = PropsUtil.get("default.admin.password"),
				urlAppend = "web/minium",
				userEmailAddress = "buyer@liferay.com");

			CommerceAcceleratorsInitializer.createNewOrderFromPendingOrders(orderType = "Test Order Type 1");

			WaitForElementPresent(locator1 = "CommerceEntry#ACTIVE_ORDER_ID");

			var order1Id = selenium.getText("CommerceEntry#ACTIVE_ORDER_ID");

			CommerceAcceleratorsInitializer.createNewOrderFromPendingOrders(orderType = "Test Order Type 2");

			WaitForElementPresent(locator1 = "CommerceEntry#ACTIVE_ORDER_ID");

			var order2Id = selenium.getText("CommerceEntry#ACTIVE_ORDER_ID");
		}

		task ("When the user goes to checkout for the Test Order Type 2 order") {
			CommerceNavigator.gotoMiniumSidebarItem(menuItem = "Catalog");

			FrontStore.addInitializerProductToCart(
				productName = "U-Joint",
				productQuantity = 1);

			CommerceOrders.selectOrderFromAccountSelector(orderId = ${order2Id});

			Button.click(button = "Checkout");
		}

		task ("Then at the shipping method step, the edited shipping option should not be available") {
			CommerceCheckout.initializersCheckout(
				newAddressAddress = "Test Address",
				newAddressCity = "Test City",
				newAddressCountry = "United States",
				newAddressName = "Address Name",
				newAddressZipCode = "Test Zip Code",
				shippingOption = "Expedited");
		}

		task ("When the user goes to checkout for the Test Order Type 1 order") {
			CommerceNavigator.gotoMiniumSidebarItem(menuItem = "Catalog");

			CommerceOrders.selectOrderFromAccountSelector(orderId = ${order1Id});

			CommerceNavigator.gotoMiniumSidebarItem(menuItem = "Catalog");

			FrontStore.addInitializerProductToCart(
				productName = "U-Joint",
				productQuantity = 1);

			CommerceOrders.selectOrderFromAccountSelector(orderId = ${order1Id});

			Button.click(button = "Checkout");
		}

		task ("Then at the shipping method step, the edited shipping option should be available") {
			CommerceCheckout.initializersCheckout(
				newAddressAddress = "Test Address",
				newAddressCity = "Test City",
				newAddressCountry = "United States",
				newAddressName = "Address Name",
				newAddressZipCode = "Test Zip Code");

			AssertElementPresent(
				key_radioOption = "Standard",
				locator1 = "CommerceEntry#RADIO_BUTTON");

			AssertElementPresent(
				key_radioOption = "Expedited",
				locator1 = "CommerceEntry#RADIO_BUTTON");
		}
	}

	@description = "This is a test for COMMERCE-8300. The store admin can upload a custom jasper print template"
	@priority = 5
	test UploadCustomJasperPrintTemplate {
		property portal.acceptance = "true";

		task ("When the sales channel detail page is reached") {
			CommerceChannels.openChannelsAdmin();
		}

		task ("And the "Select File" in the Orders box is clicked and the jasper template is chosen for being uploaded") {
			CommerceNavigator.gotoEntry(entryName = "Minium Portal");

			CommerceChannels.uploadPrintOrderTemplate(uploadFileName = "commerce_order.jrxml");

			CommerceChannels.openChannelsAdmin();
		}

		task ("Then the file name is shown next to the "Select File" button") {
			CommerceNavigator.gotoEntry(entryName = "Minium Portal");

			AssertTextEquals(
				locator1 = "CommerceOrders#PRINT_ORDER_TEMPLATE_FILE_NAME",
				value1 = "commerce_order.jrxml");
		}
	}

	@description = "This is a test for COMMERCE-6267. UsePunchOut"
	@ignore = "true"
	@priority = 3
	test UsePunchOut {

		// TODO COMMERCE-6267 UsePunchOut

	}

	@description = "This is a test for COMMERCE-8300. The store admin can view and download the jasper template"
	@priority = 3
	test ViewAndDownloadCustomJasperPrintTemplate {
		property portal.acceptance = "false";

		task ("When the sales channel detail page is reached") {
			CommerceChannels.openChannelsAdmin();
		}

		task ("And the "Select File" in the Orders box is clicked, the jasper template is chosen for being uploaded, the file name is clicked, and the preview template is shown") {
			CommerceNavigator.gotoEntry(entryName = "Minium Portal");

			CommerceChannels.uploadPrintOrderTemplate(uploadFileName = "commerce_order.jrxml");
		}

		task ("Then the jasper template is chosen for being uploaded, the file name is clicked, and the preview template is shown, and the store admin can download the template") {
			AssertTextEquals(
				locator1 = "CommerceOrders#PRINT_ORDER_TEMPLATE_FILE_NAME",
				value1 = "commerce_order.jrxml");
		}
	}

	@description = "This is a test for COMMERCE-8300. The buyer can view the order preview with the uploaded template"
	@priority = 5
	test ViewOrderPreviewWithJasperTemplate {
		property custom.properties = "company.security.strangers.verify=false${line.separator}jsonws.web.service.paths.excludes=";
		property portal.acceptance = "true";

		task ("Given a channel with a Print Order Template jasper file ") {
			CommerceChannels.openChannelsAdmin();

			CommerceNavigator.gotoEntry(entryName = "Minium Portal");

			CommerceChannels.uploadPrintOrderTemplate(uploadFileName = "commerce_order.jrxml");
		}

		task ("And a buyer user is logged in") {
			CommerceEntry.addAccountEntryUser(
				accountName = "Commerce Account",
				accountType = "Business",
				agreeToTermsAndAnswerReminderQuery = "true",
				createNewAccount = "true",
				requireReset = "false",
				userEmailAddress = "buyer@liferay.com",
				userFirstName = "Buyer",
				userLastName = "User",
				userRole = "Buyer",
				userScreenName = "buyeruser",
				userSiteMembership = "Minium");

			User.logoutPG();
		}

		task ("When an order is placed") {
			CommerceLogin.miniumLogin(
				password = PropsUtil.get("default.admin.password"),
				urlAppend = "web/minium",
				userEmailAddress = "buyer@liferay.com");

			FrontStore.addInitializerProductToCart(
				productName = "U-Joint",
				productQuantity = 1);

			CommerceCheckout.gotoCheckout();

			CommerceCheckout.checkout(
				newAddressAddress = "Test Address",
				newAddressCity = "Test City",
				newAddressCountry = "United States",
				newAddressName = "Address Name",
				newAddressZipCode = "Test Zip Code",
				shippingMethod = "Standard");

			Navigator.openWithAppendToBaseURL(urlAppend = "web/minium/placed-orders");
		}

		task ("Then the buyer user should be able to view and print the order") {
			CommerceOrders.viewJasperTemplateFromPlacedOrders();
		}
	}

}